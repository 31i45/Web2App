---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '035df0f2-083f-4258-8d08-b92b5a3ab3d7'
  PropagateID: '035df0f2-083f-4258-8d08-b92b5a3ab3d7'
  ReservedCode1: 'fe790ba0-3bc7-42ea-8b15-9e591776557b'
  ReservedCode2: 'fe790ba0-3bc7-42ea-8b15-9e591776557b'
---

# 「自繁殖单文件跨平台 Web2App」终极版需求文档（AI 一次性开发版）

## 核心特点

1. **一次性输入、独立开发**：AI 拿到文档即可从零写完整个项目，无需反复对话，免费额度完全够用。
2. **零冗余极简设计**：已压缩掉所有废话、所有可选方案、所有历史讨论；90% 的臃肿 Bug 都由 AI 乱加功能导致，严格克制功能边界。
3. **固定架构固定规范**：固定架构、固定机制、固定目录 → AI 零犹豫、零试错、零返工。
4. **代码高度统一**：没有平台分支复杂度，一套逻辑跑通全平台。

---

## 一、产品需求

1. **核心功能**：输入网页 URL，生成单文件桌面可执行应用，无依赖、无需安装，双击直接运行。
2. **自繁殖能力**：打包生成的应用本身就是母版，可再次输入新 URL 生成新的 Web2App 应用。
3. **跨平台原生支持**：一套代码原生适配 Windows / macOS / Linux 三端，产物为对应平台可执行文件。
4. **极致轻量**：空闲内存 < 100MB，单文件体积 < 10MB，冷启动速度 < 2s。
5. **表单设计**：打开母版 Web2App 时提供两个表单填写窗口——一个 URL、一个 logo 图片路径。
6. **自定义图标适配**：自动读取 logo 路径的图片文件，生成对应平台的应用图标。
7. **纯单文件交付**：产物只有一个可执行文件，不写注册表、不产生缓存目录、关闭无残留。
8. **工程质量**：代码符合软件设计的哲学和 UNIX 哲学。

## 二、开发顺序（严格按顺序执行，禁止跳步）

1. 搭建固定目录结构，仅保留核心必要文件，禁止新增任何多余目录与文件。
2. 封装系统原生 WebView 接口，统一渲染层调用，不嵌入第三方浏览器内核。
3. 开发母版启动界面，仅保留 URL 输入框、logo 路径选择控件、打包按钮三个核心元素。
4. 实现打包核心引擎：读取配置、注入图标资源、编译生成对应平台单文件可执行程序。
5. 植入自繁殖机制：将母版核心逻辑内嵌到打包产物中，实现「生成即母版」。
6. 统一跨平台编译脚本，一套指令完成三端构建，无平台差异化业务代码。
7. 核心流程验证与 Bug 修复，仅修复阻塞主链路的问题，绝对禁止新增任何功能。

## 三、禁止项（红线要求，违反即返工）

1. 禁止添加侧边栏、多标签、设置页、插件系统等任何非核心功能。
2. 禁止引入重型框架、多余第三方依赖，优先复用系统原生能力。
3. 禁止编写平台差异化业务代码，所有系统差异统一收敛到底层适配层。
4. 禁止加入引导页、开屏、更新检测、数据埋点、统计等非必要逻辑。
5. 禁止修改既定目录结构与文件命名，禁止新增非核心文件。
6. 禁止过度优化、提前优化，仅保证核心流程稳定，不做性能炫技。

## 四、验收标准（全部满足即为合格）

1. 母版启动后界面仅存在 URL 输入窗口、logo 图片路径选择窗口、打包按钮，无任何多余 UI。
2. 输入有效 URL 与 logo 路径后，点击打包 30 秒内生成对应平台的单文件可执行程序。
3. 生成的 Web2App 应用双击直接运行，完整加载目标网页，功能表现与浏览器端完全一致。
4. 生成的 Web2App 应用自身可作为母版启动，再次输入 URL 与 logo 可打包新的应用。
5. 三端产物均为单个可执行文件，体积均小于 10MB，冷启动时间均小于 2 秒。
6. 应用关闭后进程完全退出，无后台驻留、无弹窗、无任何额外行为。
7. 项目代码文件总数 ≤ 10 个，核心逻辑集中在单文件内，结构清晰无冗余。

---

## 参考

- 参考开源项目：<https://github.com/31i45/Pake/>

> AI生成